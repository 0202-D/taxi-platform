apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: taxi-platform
spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: 8282
      targetPort: 8282
  type: ClusterIP